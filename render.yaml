services:
  - type: web
    name: yolov11-multitask-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false
    autoDeploy: true
    staticPublishPath: public
    staticPublishPathType: directory